map_type: costmap

obstacle_range: 3.0 # 2.0
raytrace_range: 3.0 # 3.0

transform_tolerance: 0.3 # 0.0

robot_radius: 0.50 # 0.0
inflation_radius: 0.55 # 0.0
cost_scaling_factor: 1.0

observation_sources: laser_scan_sensor

laser_scan_sensor: {sensor_frame: hokuyo, data_type: LaserScan, topic: /scan, marking: true, clearing: true}

base_scan: {data_type: LaserScan, expected_update_rate: 0.2, observation_persistence: 0.0, marking: true, clearing: true, max_obstacle_height: 0.4, min_obstacle_height: 0.08}